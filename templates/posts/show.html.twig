{% extends 'base.html.twig' %}

{% block title %}My blog : Posts
{% endblock %}

{% block body %}
	<main>
		<div class="margin">
{# <img src="{{ post.image }}" alt="{{ post.title }}"> #}


<h1>{{ post.title }}</h1>
<p>{{ post.content }}</p>
{{ author.fullname }}

<i>{{ post.createdAt|format_datetime()  }}</i>



            
		</div>
        <div class="d-flex justify-content-center">
        <a href="{{ path('app_posts') }}"><button type="button" class="btn btn-secondary">Back</button></a>

        </div>
	</main>

{% endblock %}
